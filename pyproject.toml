[project]
name = "dataflow-connector"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.7"
dependencies = []

[tool.uv.workspace]
members = ["pre-commit"]
